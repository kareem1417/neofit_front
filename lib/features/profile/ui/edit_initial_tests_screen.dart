import 'package:flutter/material.dart';

class UpdateSnapshotScreen extends StatelessWidget {
  const UpdateSnapshotScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Update Tests"),
      ),
      body: const Center(
        child: Text("Update Screen"),
      ),
    );
  }
}